GATE="_synthetic"
EXPECT_EXIT="2"
DESCRIPTION="A gate_expect_failure region left open. errexit and the ERR trap stay suspended for the rest of the gate, which previously still reached a clean PASS with unhandled failures behind it. gate_finish now refuses to render a verdict while a region is open."
