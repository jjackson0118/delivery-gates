GATE="secrets"
EXPECT_EXIT="1"
EXPECT_RULE="private-key"
DESCRIPTION="A committed RSA private key must be caught by the specific private-key rule, not merely by the generic matcher. Two faults on one gate with different rule ids proves the gate discriminates between what it found, rather than firing on anything that looks like a secret."
