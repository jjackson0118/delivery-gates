GATE="jvm-test"
EXPECT_EXIT="1"
EXPECT_RULE="test-failure"
DESCRIPTION="A broken assertion must fail the gate, and must be attributed to test-failure rather than to a build error. The distinction matters: a failing test and a build that fell over both exit non-zero, and only one of them means the code is wrong."
