GATE="secrets"
EXPECT_EXIT="1"
SELFTEST="mismatch"
DESCRIPTION="SELF-TEST. Declares a fault the injector never applies. The gate will pass, so the harness MUST report a mismatch. If it reports \"caught\" for a fault that was never injected, the harness is lying and every other row in this run is worthless."
