GATE="jvm-test"
EXPECT_EXIT="2"
DESCRIPTION="THE IMPORTANT ONE. Every test is deleted. Gradle reports BUILD SUCCESSFUL because there is nothing to fail, and a gate reading the exit code would go green. The contract requires exit 2 -- error, not pass -- because a suite that executed nothing has measured nothing. This is the fault most pipelines would sail straight through."
